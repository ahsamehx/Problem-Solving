#include <iostream>
#include <stack>

using namespace std;


int main()
{
    int n, shift, el;
    cin >> n >> shift;
    stack<int> st;
    while (n--)
    {
        cin >> el;
        st.push(el);
    }
    
}